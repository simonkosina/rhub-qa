{
  "dns": {
    "server": {
      "create": {
        "credentials": {
          "name": "string",
          "secret": "string"
        },
        "description": "DNS server for RDU site.",
        "hostname": "localhost",
        "name": "dns-rdu",
        "owner_group_id": 1,
        "zone": "string"
      },
      "update": {
        "name": "new-name"
      }
    }
  }
}
